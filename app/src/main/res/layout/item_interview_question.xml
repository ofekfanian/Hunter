<?xml version="1.0" encoding="utf-8"?>
<!-- Single community question/tip card — company, question text, upvotes, author -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginTop="6dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="10dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/glass_background"
    app:cardCornerRadius="18dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/glass_stroke_light"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header: Logo + Company/Position -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Company Logo Circle -->
            <FrameLayout
                android:id="@+id/question_logo_container"
                android:layout_width="44dp"
                android:layout_height="44dp">

                <View
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@drawable/bg_company_logo_elegant" />

                <TextView
                    android:id="@+id/question_company_logo"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:fontFamily="@font/montserrat_bold"
                    android:gravity="center"
                    android:textColor="@android:color/white"
                    android:textSize="20sp"
                    tools:text="G" />

            </FrameLayout>

            <!-- Company + Position -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/question_company"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:fontFamily="@font/montserrat_bold"
                    android:maxLines="1"
                    android:textColor="@color/white"
                    android:textSize="15sp"
                    tools:text="Google" />

                <TextView
                    android:id="@+id/question_position"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:ellipsize="end"
                    android:fontFamily="@font/montserrat_regular"
                    android:maxLines="1"
                    android:textColor="@color/text_secondary_white"
                    android:textSize="12sp"
                    tools:text="Senior Android Developer" />

            </LinearLayout>

            <!-- Type Badge -->
            <TextView
                android:id="@+id/question_difficulty"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_badge_blue"
                android:fontFamily="@font/montserrat_semibold"
                android:paddingStart="10dp"
                android:paddingTop="4dp"
                android:paddingEnd="10dp"
                android:paddingBottom="4dp"
                android:textSize="11sp"
                tools:text="Question" />

        </LinearLayout>

        <!-- Question Text -->
        <TextView
            android:id="@+id/question_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:ellipsize="end"
            android:fontFamily="@font/montserrat_regular"
            android:lineSpacingExtra="3dp"
            android:maxLines="3"
            android:textColor="@color/white"
            android:textSize="14sp"
            tools:text="What is the difference between Activity and Fragment lifecycle? How do you handle configuration changes properly?" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="10dp"
            android:background="@color/divider_light" />

        <!-- Footer: Upvotes + Spacer + Username -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/upvote_button"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:padding="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/upvote"
                android:src="@drawable/ic_arrow_upward"
                app:tint="@color/primary_blue" />

            <TextView
                android:id="@+id/question_upvotes"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:fontFamily="@font/montserrat_semibold"
                android:textColor="@color/primary_blue"
                android:textSize="13sp"
                tools:text="47" />

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/question_user"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/montserrat_semibold"
                android:textColor="@color/text_secondary_white"
                android:textSize="12sp"
                tools:text="Alex R." />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>

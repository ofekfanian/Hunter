<?xml version="1.0" encoding="utf-8"?>
<!-- Reusable header bar — hamburger menu, screen title, and icon -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/gradient_dark_purple" />

    <ImageView
        android:id="@+id/header_IMG_circuit"
        android:layout_width="220dp"
        android:layout_height="240dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="-50dp"
        android:layout_marginEnd="-60dp"
        android:alpha="0.22"
        android:contentDescription="@string/app_name"
        android:src="@drawable/logo_hunter_line_art" />

    <!-- Decorative Logo (Bottom Left) -->
    <ImageView
        android:id="@+id/background_IMG_bottom"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="-50dp"
        android:layout_marginBottom="-60dp"
        android:alpha="0.14"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop"
        android:src="@drawable/logo_hunter_line_art" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            app:elevation="0dp">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingTop="40dp"
                android:paddingBottom="12dp">

                <ImageButton
                    android:id="@+id/header_menu_button"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_menu"
                    app:tint="@color/white"
                    android:scaleType="center"
                    android:contentDescription="@string/menu"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent" />

                <TextView
                    android:id="@+id/header_title"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    tools:text="@string/app_name"
                    android:textColor="@color/white"
                    android:textSize="24sp"
                    android:fontFamily="@font/montserrat_bold"
                    android:letterSpacing="0.02"
                    android:gravity="center"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:shadowColor="@color/shadow_medium"
                    android:shadowDx="0"
                    android:shadowDy="2"
                    android:shadowRadius="4"
                    app:layout_constraintStart_toEndOf="@id/header_menu_button"
                    app:layout_constraintEnd_toStartOf="@id/header_screen_icon"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent" />

                <ImageView
                    android:id="@+id/header_screen_icon"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:scaleType="fitCenter"
                    app:tint="@color/white"
                    android:contentDescription="@string/icon"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent" />

            </androidx.constraintlayout.widget.ConstraintLayout>

        </com.google.android.material.appbar.AppBarLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:background="@color/white"
            android:alpha="0.3" />

    </LinearLayout>

</FrameLayout>
